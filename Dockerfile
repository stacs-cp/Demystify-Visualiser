FROM node:latest-alpine
# RUN apt-get update
# RUN apt-get install --no-cache python3 py3-pip git
# RUN git clone https://github.com/mmcilree/Demystify-Visualiser.git /home/DemystifyVisualiser
# #WORKDIR /home/DemystifyVisualiser/client
# #RUN npm install && npm run build
# WORKDIR /home/DemystifyVisualiser/server
# RUN python3 -m venv venv && source venv/bin/activate && pip3 install -r requirements.txt